apiVersion: arubacloud.crossplane.io/v1alpha1
kind: Database
metadata:
  name: appdb
  namespace: crossplane-system
spec:
  forProvider:
    name: appdb
    projectIdRef:
      name: my-project
    dbaasIdRef:
      name: my-db-cluster
  providerConfigRef:
    name: default
